from database.DAO import DAO

# ---------------------------------------------- Modello di test (extra) ---------------------------------------------

allObjects = DAO.getAllNodes()
print(len(allObjects))
